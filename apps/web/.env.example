# Aegis frontend configuration.
# Base URL of the Aegis backend REST + SSE API (no trailing slash).
# The client falls back to http://localhost:4000 when unset.
VITE_API_BASE_URL=http://localhost:4000
